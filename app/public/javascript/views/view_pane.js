// Generated by CoffeeScript 1.6.1
(function() {
  var $, ViewPane;

  $ = Loader.require('jQuery');

  ViewPane = (function() {

    function ViewPane() {}

    ViewPane.prototype.initialize = function() {
      this.views = {};
      this.pane = $('<div>');
      return this.pane.appendTo($('body'));
    };

    ViewPane.prototype.resetView = function() {
      return this.pane.empty();
    };

    ViewPane.prototype.getView = function(viewName) {
      if (!this.views[viewName]) {
        this.views[viewName] = Loader.require("views/" + viewName);
      }
      return this.views[viewName];
    };

    ViewPane.prototype.render = function(viewName, config, target) {
      var view;
      if (config == null) {
        config = {};
      }
      if (target == null) {
        target = this.pane;
      }
      view = this.getView(viewName);
      return view(target, config);
    };

    return ViewPane;

  })();

  Loader.register('views/ViewPane', new ViewPane());

}).call(this);
