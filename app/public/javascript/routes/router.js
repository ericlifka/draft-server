// Generated by CoffeeScript 1.6.1
(function() {
  var Router;

  Router = (function() {

    function Router() {}

    Router.prototype.initialize = function() {
      return this.routes = {};
    };

    Router.prototype.getRoute = function(routeName) {
      if (!this.routes[routeName]) {
        this.routes[routeName] = Loader.require("routes/" + routeName);
      }
      return this.routes[routeName];
    };

    Router.prototype.goTo = function(routeName, action) {
      var route;
      if (action == null) {
        action = 'index';
      }
      route = this.getRoute(routeName);
      return route[action].call();
    };

    return Router;

  })();

  Loader.register('routes/Router', new Router());

}).call(this);
